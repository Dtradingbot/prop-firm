{
  "name": "propfirmhub",
  "version": "1.0.0",
  "description": "PropFirmHub — Combined server for Hostinger single domain",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "setup": "cd backend && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.3",
    "dotenv": "^16.4.5"
  }
}
